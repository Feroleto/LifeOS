NODE_ENV=test
PORT=3001

# Dedicated e2e test database — wiped on every suite, never point it at the development one.
DATABASE_URL="postgresql://lifeos:lifeos@localhost:5433/lifeos_test?schema=public"
